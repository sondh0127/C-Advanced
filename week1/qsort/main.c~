#include <stdio.h>
#include <stdlib.h>
#include <time.h>
#include <string.h>
#include "sort.h"

int main()
{
	FILE *fp = fopen("phonebook.txt", "r");
	if(fp == NULL){
		printf("Error while opening the file.\n");
		return 1;
	}
	int ch;
	int size = 0;
	while((ch = fgetc(fp)) != 0) {
		if(ch == '\n')
			size++;
	}
	printf("Size of file: %d Phone", size);
	fseek(fp, 0,SEEK_SET);
	//Phone arr[size];
	//int i = 0;
	//while(!feof(fp)) {
	//fscanf(fp, "%[^\t]\t%[^\n]\n", arr[i].name, arr[i].phone);
	//printf("%s\t%s\n",arr[i].name, arr[i].phone);
//i++;
	//}
	

	fclose(fp);
    return 0;
}
